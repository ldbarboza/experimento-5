// Next.js 14 only loads `next.config.js` / `next.config.mjs`; a TypeScript
// config file is rejected at build time, so the types come from JSDoc instead.
/** @type {import('next').NextConfig} */
const nextConfig = {};

export default nextConfig;
